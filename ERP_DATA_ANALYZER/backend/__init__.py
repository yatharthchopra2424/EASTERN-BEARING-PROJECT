# This file can be empty or used for package-level initialization if needed.
import logging

# Configure root logger level based on config if desired,
# although specific loggers are configured in utilities.py
# from .config import Config
# logging.basicConfig(level=Config.LOG_LEVEL)

# print("streamlit_app package initialized") # Optional debug print